import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.okode.demo',
  appName: 'demo-bouncing',
  webDir: 'dist/demo-bouncing',
  bundledWebRuntime: false
};

export default config;
